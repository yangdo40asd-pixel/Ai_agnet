# AI 인사이트 리포트 (AI Insight Report) - 프로젝트 개발 마스터 플랜

## 1. 프로젝트 개요 (우리의 프로젝트는 무엇인가?)
본 프로젝트는 **"사용자가 던진 키워드(테마) 하나로, AI가 관련 종목을 찾고 재무/뉴스 데이터를 모아 의미 있는 해석과 함께 정돈된 리포트로 보여주는 웹 서비스"**입니다.
기존 증권 앱들이 종목코드 중심의 단순 수치 제공에 그치는 것과 달리, 일반 투자자들이 관심 있는 '테마'를 중심으로 쉽고 직관적인 인사이트를 얻을 수 있도록 돕는 것을 목표로 합니다.

## 2. 핵심 구현 목표 (무엇을 할 것인가?)
1. **테마 기반 종목 탐색**: "AI 반도체", "2차전지" 등의 키워드 입력 시 관련성 높은 종목 자동 검색
2. **실시간 금융 데이터 파이프라인**: 종목의 주가 및 주요 재무지표(PER, PBR, ROE 등), 최신 뉴스 헤드라인 수집
3. **AI 기반 데이터 종합 및 해석**: 수집된 Raw 데이터를 단순 나열하지 않고, LLM을 통해 "이 수치와 뉴스가 의미하는 바(밸류에이션, 위험요인 등)"를 분석
4. **대화형(챗봇) 단일 UI 제공**: 복잡한 대시보드 대신, 검색 한 번에 리포트가 도출되는 심플한 Streamlit 웹 인터페이스 구축

---

## 3. 필요 라이브러리 및 API 후보군

### 3.1 파이썬 필수 설치 라이브러리 (Dependencies)
프로젝트 실행을 위해 `requirements.txt`에 포함되어야 할 핵심 패키지들입니다.

| 분류 | 라이브러리명 | 역할 및 채택 사유 | 대체 후보군 |
| :--- | :--- | :--- | :--- |
| **Web UI** | `streamlit` | 파이썬만으로 빠르게 챗봇/대시보드 UI를 구축하기 위함 | `Gradio` (단순하지만 커스텀 제약), `Flask/Django` (프론트엔드 작업 과다) |
| **금융 데이터** | `yfinance` | 글로벌 및 국내 주식 주가, 재무 지표를 무료로 크롤링 없이 가져올 수 있는 표준 API | `pykrx` (국내 주식 특화 데이터가 더 필요할 경우 병행 사용 고려) |
| **뉴스 크롤링** | `requests`, `beautifulsoup4` | 정적 웹페이지(뉴스 검색 결과)의 HTML을 파싱하여 제목/링크 추출 | `Selenium` (너무 무거움. 크롤링 차단 시에만 최후의 수단으로 고려) |
| **AI 통합** | `langchain`, `crewai` | LLM과 외부 툴(검색, 코드 실행 등)을 엮어서 에이전트를 구성하기 위한 뼈대 | `LlamaIndex` (RAG 특화이므로 본 프로젝트에는 LangChain이 더 적합) |
| **데이터 처리** | `pandas` | 수집된 다수의 재무 데이터를 표 형태로 가공하고 전처리 | 내장 `json`, `csv` 모듈 (한계가 뚜렷함) |
| **환경 설정** | `python-dotenv` | API 키를 소스코드 외부에 안전하게 보관 | `os.environ` 하드코딩 (보안 취약) |

### 3.2 연동할 외부 API (API Candidates)
AI 추론과 데이터의 신뢰성을 높이기 위해 회원가입 및 키 발급이 필요한 API들입니다.

1. **LLM (언어 모델) API** 
   * **[우선 후보] OpenAI API (GPT-4o-mini)**: 가장 안정적이고 프롬프트 제어가 잘 됨. 요금도 매우 저렴함.
   * **[서브 후보] Groq API (Llama-3)**: 추론 속도가 압도적으로 빨라 로딩(Latency) 단축에 유리.
2. **뉴스 수집 API**
   * **[우선 후보] 네이버 검색 API (News)**: BeautifulSoup 크롤링이 네이버 정책에 의해 차단될 위험을 방지하기 위해, 무료 개발자 API(일 25,000회) 발급 권장.
3. **추가 금융 API (선택)**
   * **금융감독원 DART API**: yfinance 데이터가 부족할 경우, 국내 기업의 정확한 공시/재무제표 데이터를 보완하기 위해 사용.

---

## 4. 앞으로 만들어야 할 것들 (구체적 로드맵)

프로젝트 성공을 위해 앞으로 우리가 순차적으로 작성하고 조립해야 할 **모듈(코드 파일) 목록과 계획**입니다.

### 📍 Phase 1: 개발 환경 세팅 및 기초 공사
* [ ] 가상환경 세팅 및 `requirements.txt` 패키지 설치
* [ ] OpenAI, 네이버 검색 API 가입 및 `.env` 파일에 API Key 발급·저장
* [ ] `.gitignore` 작성 (보안 유지)

### 📍 Phase 2: 데이터 수집 모듈 개발 (Data Pipeline PoC)
AI에게 먹이를 줄 가장 중요한 데이터 수집 파이프라인부터 완성해야 합니다.
* [ ] `finance_api.py`: 티커(종목코드)를 넣으면 yfinance를 통해 현재가, PER, PBR 등을 JSON 형태로 반환하는 함수 개발
* [ ] `news_scraper.py`: 테마 키워드(예: "AI 반도체")를 넣으면 네이버 API나 BS4를 통해 최신 뉴스 헤드라인 Top 5를 리스트로 반환하는 함수 개발
* [ ] **중간 점검**: 위 두 코드가 터미널에서 텍스트를 제대로 가져오는지 교수님 시연 및 1차 검증

### 📍 Phase 3: AI 분석 에이전트 및 프롬프트 설계
데이터를 해석할 AI 두뇌를 만듭니다.
* [ ] `agent.py`: LangChain 기반으로 LLM 호출 로직 작성
* [ ] **프롬프트 엔지니어링**: AI가 헛소리(할루시네이션)를 하지 않도록 지시문 설계. 
  *(예: "너는 제공된 JSON 재무 데이터와 뉴스 텍스트만 활용해서 리포트를 써야 해. 매수/매도 추천은 하지 마.")*
* [ ] **데이터 결합 파이프라인**: Phase 2에서 만든 금융/뉴스 데이터를 AI의 입력값(Context)으로 주입하는 로직 완성

### 📍 Phase 4: Streamlit 웹 UI 통합 및 연동
사용자가 실제로 보게 될 화면을 만들고 뒤단(Backend)과 연결합니다.
* [ ] `app.py`: Streamlit을 활용한 챗봇 UI 뼈대 작성
* [ ] **비동기 처리/로딩 시각화**: 데이터를 모으고 AI가 글을 쓰는 대기 시간(약 5~10초) 동안 스피너(Spinner)나 진행 상태바 구현
* [ ] 백엔드(Phase 3)의 결과를 받아 마크다운(표, 리스트 등)으로 예쁘게 렌더링

### 📍 Phase 5: 리팩토링 및 고도화
* [ ] 에러 처리: 검색된 종목이 없거나, API 호출 제한이 걸렸을 때의 안내 메시지 처리
* [ ] 테마-종목 맵핑 고도화: 키워드 검색 시 정확한 관련주를 매칭하기 위한 로직 개선
* [ ] 최종 테스트 및 배포 준비
